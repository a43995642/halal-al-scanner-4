{
  "name": "halal-scanner",
  "private": true,
  "version": "2.2.0",
  "description": "A React application that uses Gemini AI to scan food ingredients and determine if they are Halal, Haram, or Doubtful.",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "start": "node dist/server.js",
    "build": "vite build && esbuild server.ts --bundle --platform=node --target=node18 --format=esm --external:lightningcss --outfile=dist/server.js",
    "type-check": "tsc -b",
    "lint": "eslint .",
    "preview": "vite preview",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "ensure-android": "node scripts/ensure-android-structure.js",
    "set-icon": "node scripts/set-android-icon.js",
    "clean-native": "node scripts/clean-native.js",
    "setup-android": "npx cap telemetry off && (npx cap add android || echo 'Android platform already exists')",
    "fix-gradle": "node scripts/fix-gradle-wrapper.js",
    "force-java": "node scripts/force-java-compatibility.js",
    "ensure-vars": "node scripts/ensure-variables-gradle.js",
    "get-sha1": "node scripts/get-sha1.js",
    "create-release-key": "node scripts/create-release-key.js",
    "refresh-android": "npm run build && npm run clean-native && npm run setup-android && npx cap sync && npm run ensure-vars && npm run fix-gradle && npm run force-java && npm run set-icon",
    "build-apk": "npm run get-sha1 && npm run build && npm run clean-native && npm run setup-android && npx cap sync && npm run ensure-vars && npm run fix-gradle && npm run force-java && npm run set-icon && node update-manifest.js && node scripts/smart-build.js debug",
    "build-release-apk": "npm run build && npm run clean-native && npm run setup-android && npx cap sync && npm run create-release-key && npm run ensure-vars && npm run fix-gradle && npm run force-java && npm run set-icon && node update-manifest.js && node scripts/smart-build.js both",
    "build-aab": "npm run build && npm run clean-native && npm run setup-android && npx cap sync && npm run create-release-key && npm run ensure-vars && npm run fix-gradle && npm run force-java && npm run set-icon && node update-manifest.js && node scripts/smart-build.js bundle",
    "build-all": "npm run build && npm run clean-native && npm run setup-android && npx cap sync && npm run create-release-key && npm run ensure-vars && npm run fix-gradle && npm run force-java && npm run set-icon && node update-manifest.js && node scripts/smart-build.js both"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/clipboard": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@codetrix-studio/capacitor-google-auth": "^3.3.0",
    "@google/genai": "^1.42.0",
    "@revenuecat/purchases-capacitor": "^7.0.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase": "^12.12.0",
    "firebase-admin": "^13.8.0",
    "html-to-image": "^1.11.13",
    "postcss": "^8.4.38",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@eslint/js": "^10.0.1",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "eslint": "^10.0.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "tsx": "^4.21.0",
    "typescript-eslint": "^8.56.1"
  },
  "overrides": {
    "@codetrix-studio/capacitor-google-auth": {
      "@capacitor/core": "^6.0.0"
    }
  }
}
